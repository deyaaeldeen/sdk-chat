{
  "mcp": {
    "servers": {
      "sdk-chat": {
        "type": "stdio",
        "command": "dotnet",
        "args": ["run", "--project", "${workspaceFolder}/Microsoft.SdkChat/Microsoft.SdkChat.csproj", "--", "mcp"]
      }
    }
  }
}
