{
  "mcp": {
    "servers": {
      "sdk-chat": {
        "type": "stdio",
        "command": "docker",
        "args": [
          "run", "--rm", "-i",
          "-v", "${userHome}/.copilot:/root/.copilot:ro",
          "sdk-chat:latest", "mcp"
        ]
      }
    }
  }
}
